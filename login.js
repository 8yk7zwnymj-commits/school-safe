document.getElementById("loginForm").addEventListener("submit",async e=>{e.preventDefault();const email=document.getElementById("email").value.trim().toLowerCase(),pw=document.getElementById("password").value,msg=document.getElementById("msg"),hash=await hashPassword(pw),user=getUsers().find(u=>u.email===email&&u.passwordHash===hash);if(!user){msg.className="form-msg show error";msg.textContent="อีเมลหรือรหัสผ่านไม่ถูกต้อง";return}setCurrent({email:user.email});location.href="index.html"});