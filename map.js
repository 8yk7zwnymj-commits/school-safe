requireLogin();fillUser();const info={nurse:["ห้องพยาบาล","ใช้เป็นจุดติดต่อเมื่อมีผู้ไม่สบายหรือบาดเจ็บ และควรทำตามแนวทางของโรงเรียน"],assembly:["จุดรวมพล","พื้นที่ที่โรงเรียนกำหนดสำหรับรวมกลุ่มหลังการอพยพ ควรตรวจสอบจุดจริงกับครู/โรงเรียน"],exit:["ทางเข้า-ออก","ควรรู้เส้นทางหลักและเส้นทางสำรองก่อนเกิดเหตุ ไม่วางสิ่งของกีดขวางบริเวณทางออก"]};document.querySelectorAll("[data-place]").forEach(b=>b.addEventListener("click",()=>{const x=info[b.dataset.place],box=document.getElementById("placeInfo");box.classList.add("show");box.innerHTML=`<h3>${x[0]}</h3><p class="small muted">${x[1]}</p>`}));